const buttons = require('./buttons.handlebars');
const carousel = require('./carousel.handlebars');
const imageContainer = require('./imageContainer.handlebars');

module.exports = {
    buttons,
    carousel,
    imageContainer
}